{
  "name": "saas-gw-api",
  "version": "1.0.0",
  "description": "API Gateway with Lambda backend requiring API key authentication",
  "scripts": {
    "deploy": "AWS_PROFILE=tx-sandbox sls deploy --stage dev",
    "deploy:prod": "AWS_PROFILE=tx-sandbox sls deploy --stage prod",
    "deploy:verbose": "AWS_PROFILE=tx-sandbox sls deploy --stage dev --verbose",
    "remove": "AWS_PROFILE=tx-sandbox serverless remove",
    "cloudfront:info": "AWS_PROFILE=tx-sandbox aws cloudformation describe-stacks --stack-name saas-gw-api-dev --query 'Stacks[0].Outputs[?OutputKey==`CloudFrontDomainName`]' --output table"
  },
  "devDependencies": {
    "serverless": "^3.32.2",
    "serverless-python-requirements": "^6.0.0"
  }
} 